# Nixpacksビルド設定（Railpack secrets問題を回避）
[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python discord_bot_server.py"
